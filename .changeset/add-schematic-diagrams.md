---
'mermaid': minor
---

feat: add schematic diagrams (`schematic-beta`) with top-level `in`/`out`/`inout` ports, logic gate primitives, `-->` nets with auto-assigned gate inputs, `direction`, and accessibility metadata.

---
'mermaid': minor
---

feat: add `module`/`end` declarations, `group @side`, and port kinds (`: clock`/`: reset`) to schematic diagrams. Instances of a declared module get the same dotted-port validation and input auto-assignment as built-in primitives. Rendering doesn't yet place ports by side — module instances still draw as plain boxes.
